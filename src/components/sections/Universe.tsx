import { SectionShell } from '../layout/SectionShell'

export function Universe() {
  return (
    <SectionShell
      id="universe"
      eyebrow="Technical Universe"
      title="Where iOS Engineering Meets Adjacent Disciplines"
      pendingNote="Orbiting-node diagram (Swift → SwiftUI → Concurrency → Architecture, Core ML → On-device AI, etc.) will be built once the skill emphasis is confirmed from your resume."
    />
  )
}
