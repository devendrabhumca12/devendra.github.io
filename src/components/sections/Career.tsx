import { SectionShell } from '../layout/SectionShell'

export function Career() {
  return (
    <SectionShell
      id="career"
      eyebrow="11+ Years"
      title="Career Journey"
      pendingNote="Timeline (role, company, period, key work) will be built from the resume once provided."
    />
  )
}
