import { SectionShell } from '../layout/SectionShell'

export function Contact() {
  return (
    <SectionShell
      id="contact"
      eyebrow="Get in Touch"
      title="Contact"
      pendingNote="Public contact links (email, LinkedIn, GitHub) pending confirmation of which should be shown."
    />
  )
}
